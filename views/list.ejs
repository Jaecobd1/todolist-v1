<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Todo List</title>
</head>
<body>
    <% if (kindOfDay === "Monday"){ %>
    <h1 style="color: purple"><%= kindOfDay%> List</h1>
   <% } else { %>
    <h1 style="color:blue"><%= kindOfDay=%> List </h1>
   <% } %>
</body>
</html>